<ul>

<% @notes.each do |note| %>
  <li>
    <%= note.content %>
    <%= link_to image_tag('trash-can-icon.jpg', height: 20), note_url(note), method: 'delete', confirm: 'Really?' %>
    <%= link_to image_tag('pencil.png', height: 20), edit_note_url(note) %>
  </li>
<% end %>

</ul>
